[project]
name = "utilz"
version = "0.1.0"
description = "My local utility library"
requires-python = ">=3.13"

[build-system]
requires = ["setuptools>=40.8.0"]
build-backend = "setuptools.build_meta"